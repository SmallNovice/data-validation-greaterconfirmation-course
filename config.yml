title: Data validation--->confirmation
description: ordinary validation
template:
  repo: data-validation-greaterconfirmation-template
  name: data-validation-greaterconfirmation
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
